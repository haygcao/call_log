name: call_log_example
description: Demonstrates how to use the call_log plugin.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  workmanager: ^0.5.0
  permission_handler: ^11.3.1
  sim_card_info: ^1.0.2
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  call_log:
    path: ../

flutter:
  uses-material-design: true